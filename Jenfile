@Library('my-shared-lib') _

pipeline {
    agent any

    stages {
        stage('Update Component Properties') {
            steps {
                script {
                    somePipeline()
                }
            }
        }
    }
}
